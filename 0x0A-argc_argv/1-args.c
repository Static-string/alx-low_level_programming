#include <stdio.h>

/**
 * main - program entry point
 *
 * @argc: argument count
 * @argv: argument value
 * Return: Always (0);
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
